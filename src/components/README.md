# src/components directory
This should have reusable UI components.

Involved here should be:
- Buttons
- Inputs
- Modals

Components vs Hooks:
Components are reusable UI elements.
Hooks are reusable logic.